{% extends "::principal_layout.html.twig" %}

{% block title %}ClientUserBundle:Client:getClients{% endblock %}

{% block body %}

    <h1>Cette partie n'est pas dans layout global</h1>
    {# !! ???
    {# On affiche les messages flash que définissent les contrôleurs du bundle #}
    {% for key, message in app.session.flashbag.all() %}
        <div class="alert alert-{{ key }}">
            {{ message|trans({}, 'FOSUserBundle') }}
        </div>
    {% endfor %}
    <hr>

    {# On définit ce block, dans lequel vont venir s'insérer les autres vues du bundle #}
    {% block fos_user_content %}
    {% endblock fos_user_content %}

    {% for client in liste_clients %}
        <li>{{ client.name }}    {{ client.dateCreate|date('d-m-y h:m:s') }}</li>
        {% endfor %}

    {#        <li>{{ liste_clients.name }} {{ liste_clients.dateCreate|date('d-m-y h:m:s') }}</li>#}
    <h1>Fin de la partie qui change en fonction de l'onglet</h1>
{% endblock %}

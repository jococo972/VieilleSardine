get_clients:
    pattern: /clients
    defaults: { _controller: TestHelloBundle:Client:getClients }
    
add_client:
    pattern: /registry
    defaults: { _controller: TestHelloBundle:Client:addClient }
